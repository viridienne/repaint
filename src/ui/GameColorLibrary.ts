// Placeholder — Game Color Library integration reserved for v2.
// When palette data is available, this component will render labeled swatches.
export class GameColorLibrary {
  constructor() {
    // DOM is static from index.html; no dynamic rendering in v1.
  }
}
